import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
    paddingLeft: 25,
    paddingRight: 25,
  },

  inputsContainer: {
    marginBottom: 40,
  },
});

export {styles};
